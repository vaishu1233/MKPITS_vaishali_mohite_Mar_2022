#include<stdio.h>
#include<conio.h>
void main()
while(beg<=end);
mid=big+end/2;
if(ser==a[mid])
{
    present
    break;
}else
if(ser<=[mid])
    end=mid-1;
{
    end=mid-1;
}
else
{
    beg=mid=1;
}
